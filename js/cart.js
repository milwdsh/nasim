document.addEventListener("DOMContentLoaded",()=>{const list=document.getElementById("cartList"),subtotalEl=document.getElementById("subtotal"),totalEl=document.getElementById("total"),empty=document.getElementById("emptyCart");if(!list)return;function render(){let c=cart();empty.style.display=c.length?"none":"block";list.innerHTML=c.map((x,i)=>`<div class="cart-item"><div class="cart-thumb ${x.cover_url?"":"art-modern"}">${x.cover_url?`<img src="${x.cover_url}" style="width:100%;height:100%;object-fit:cover;border-radius:14px">`:""}</div><div class="cart-meta"><h3>${x.name}</h3><small>${money(x.price)}</small></div><div class="qty"><button data-minus="${i}">−</button><b>${x.quantity}</b><button data-plus="${i}">+</button></div></div>`).join("");let sub=c.reduce((s,x)=>s+x.price*x.quantity,0);subtotalEl.textContent=money(sub);totalEl.textContent=money(sub);list.querySelectorAll("[data-plus]").forEach(b=>b.onclick=()=>{c[b.dataset.plus].quantity++;saveCart(c);render()});list.querySelectorAll("[data-minus]").forEach(b=>b.onclick=()=>{let i=b.dataset.minus;c[i].quantity--;if(c[i].quantity<=0)c.splice(i,1);saveCart(c);render()})}render()});